for i in range(10):
    if i % 2 == 0:  # se for par
        continue  # pula
    print(f"Número ímpar: {i}")
"Esse código imprime apenas os números ímpares de 0 a 9, usando o comando 'continue' para pular os números pares. É uma maneira eficiente de filtrar os números que queremos processar."
